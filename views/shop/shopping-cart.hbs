<p>
{{# if products}}
    <div class="row">
        <div class="col-lg-6 col-6 ">
            <u1 class="list-group">
                {{# each products}}
                    <li class = "list-group-item">
                        <strong>{{this.item.title}}  <span class="badge badge-success">${{this.price}}</span></strong>
                        <button class="btn btn-secundary btn-sm" type="button">
                            Remove
                        </button>
                        <span class="badge badge-secondary float-right">{{this.qty}}</span>
                    </li>
                {{/each}}
            </u1>
        </div>
    </div>
    <p>
    <div class="row">
        <div class="col-lg-4 col-6 ">
            <strong> Total: ${{this.totalPrice}} </strong>
        </div>
    </div>
    <hr>
    <div class="row">
        <div class="col-lg-6 col-6 ">
            <button type="button" class="btn btn-success"> checkout</button>
        </div>
    </div>
{{else}}
    <div class="row">
        <div class="col-lg-6 col-6 ">
            <h2>No items in cart</h2>
        </div>
    </div>
{{/if}}